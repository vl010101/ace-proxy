Конечно, вот красивая и понятная инструкция, готовая для добавления в README.md вашего репозитория.

Инструкция по установке и запуску
Это руководство поможет вам развернуть приложение HTTPAceProxy с помощью Docker. Для удобного управления контейнерами мы также установим Portainer.

Часть 1: Подготовка сервера и установка Portainer 🐧
Portainer — это удобный веб-интерфейс для управления вашими Docker-контейнерами.

### Шаг 1: Обновление системы
Сначала обновим пакеты вашей системы до последних версий.

Bash

sudo apt update && sudo apt upgrade -y
### Шаг 2: Установка Docker
Нам понадобится Docker для запуска контейнеров.

Bash

sudo apt install -y docker.io docker-compose
После установки включаем и запускаем сервис Docker.

Bash

sudo systemctl enable docker --now
### Шаг 3: Запуск Portainer
Создадим специальный том для хранения данных Portainer и запустим контейнер.

Bash

sudo docker volume create portainer_data
Рекомендуется использовать защищенное HTTPS соединение.

Bash

sudo docker run -d \
  -p 8000:8000 \
  -p 9443:9443 \
  --name portainer \
  --restart=always \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v portainer_data:/data \
  portainer/portainer-ce:latest
### Шаг 4: Первоначальная настройка Portainer
Откройте браузер и перейдите по адресу, используя IP вашего сервера:
https://<IP_вашего_сервера>:9443

При первом входе вам будет предложено создать пароль для администратора. После этого выберите "Управлять локальной средой Docker" ("Manage the local Docker environment"), и вы попадете в панель управления.

Часть 2: Установка и запуск HTTPAceProxy 🚀
Теперь, когда Docker и Portainer готовы, можно запустить основное приложение.

### Шаг 1: Сборка Docker-образа из GitHub
Эта команда автоматически скачает исходный код из этого репозитория и соберет из него готовый Docker-образ. Вам не нужно ничего клонировать вручную.

Bash

docker build -t vl010101/httpaceproxy https://github.com/vl010101/httpaceproxy.git
Дождитесь сообщения Successfully built ....

### Шаг 2: Запуск контейнера HTTPAceProxy
Теперь запустим контейнер из свежесобранного образа. Он будет работать в фоновом режиме и автоматически перезапускаться.

Bash

docker run -d --privileged \
  -p 10001:62062 \
  -p 20001:6878 \
  -p 30001:8081 \
  -p 50001:8621 \
  -e PGID=0 \
  -e PUID=0 \
  --restart always \
  --memory="1g" \
  --name TVP1 \
  vl010101/httpaceproxy
### Шаг 3: Доступ к сервису 🌐
Приложение запущено! Вы можете использовать следующие ссылки:

Основной плейлист для плеера (VLC, IPTV и т.д.):
http://<IP_вашего_сервера>:30001/torrenttv/playlist.m3u

Веб-интерфейс Ace Stream:
http://<IP_вашего_сервера>:20001/webui/app/WkRi4UeG8p/server

Страница статистики:
http://<IP_вашего_сервера>:30001/stat

Обновление приложения 🔄
Если в репозитории появятся обновления, процесс обновления очень прост:

Пересоберите образ, чтобы скачать последнюю версию кода:

Bash

docker build -t vl010101/httpaceproxy https://github.com/vl010101/httpaceproxy.git
Остановите и удалите старый контейнер:

Bash

docker stop TVP1 && docker rm TVP1
Запустите новый контейнер той же командой, что и в Шаге 2.
